apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "stats-api.fullname" . }}
  labels:
    {{- include "stats-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
