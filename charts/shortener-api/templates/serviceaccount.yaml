{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "shortener-api.fullname" . }}
  labels:
    {{- include "shortener-api.labels" . | nindent 4 }}
{{- end }}
